# Standalone cargo-fuzz workspace (nightly). Excluded from the root workspace in
# the top-level Cargo.toml; the empty [workspace] table below makes this its own
# workspace root so the two never conflict.
[package]
name = "engram-fuzz"
version = "0.0.0"
edition = "2021"
publish = false

[package.metadata]
cargo-fuzz = true

[workspace]

[dependencies]
libfuzzer-sys = "0.4"
arbitrary = { version = "1", features = ["derive"] }
engram-core = { path = "../crates/engram-core" }
engram-storage = { path = "../crates/engram-storage" }

[[bin]]
name = "record_codec"
path = "fuzz_targets/record_codec.rs"
test = false
doc = false
bench = false

[[bin]]
name = "wal_reader"
path = "fuzz_targets/wal_reader.rs"
test = false
doc = false
bench = false

[[bin]]
name = "btree_ops"
path = "fuzz_targets/btree_ops.rs"
test = false
doc = false
bench = false

[profile.release]
debug = 1
